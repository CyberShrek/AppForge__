interface SelectInputConfig{
    multiple?: boolean
    search?: boolean
    showCodes?: boolean
    disableSelectAll?: boolean
    required?: boolean
    maxValues?: number
}

interface DateInputConfig {
    maxDays?: number
    defaultRange?: DateRange
}