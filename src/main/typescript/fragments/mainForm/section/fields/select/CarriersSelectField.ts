import {SelectField} from "./SelectField"

export class CarriersSelectField extends SelectField{
    constructor(location: FragmentLocation, configElement: HTMLElement) {
        super(location,configElement)
    }
}